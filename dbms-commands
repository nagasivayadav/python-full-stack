commands

create table student(
    student_id int primary key,
    student_name varchar(30),
    branch varchar(20),
    age int,
    email varchar(50) unique
);

insert into student values(101,'nagasiva','CSE',20,'nagasiva@gmail.com');
insert into student values(102,'reddy','ECE',21,'reddy@gmail.com');
insert into student values(103,'mohan','EEE',22,'mohan@gmail.com');

select * from student;

select student_name,branch from student;

select * from student where branch='CSE';

update student
set branch='IT'
where student_name='reddy';

delete from student
where student_id=103;

alter table student
add phone varchar(15);

alter table student
modify phone varchar(20);

alter table student
rename column phone to mobile_no;

alter table student
drop column mobile_no;

rename table student to students;

select * from students;

start transaction;

insert into students values(104,'kiran','AIML',20,'kiran@gmail.com');

rollback;

start transaction;

insert into students values(104,'kiran','AIML',20,'kiran@gmail.com');

commit;

grant select on college.students to 'user1'@'localhost';

revoke select on college.students from 'user1'@'localhost';

truncate table students;

drop table students;

drop database college;

# creat views
create table st(name varchar(20),marks tinyint);
insert into st values('naga','89');
insert into st values('siva','87');
insert into st values('roda','83');
insert into st values('somu','80');

create view marks as select * from st
where marks='87';
use college;

#joins

create table empp(emp_id int primary key,
name varchar(20));
insert into empp values(101,'naga');
insert into empp values(102,'siva');
insert into empp values(103,'roda');
insert into empp values(104,'nagasiva');
create table course(
id int primary key,
course_name varchar(20));
insert into course values(101,'java');
insert into course values(102,'sql');
insert into course values(103,'c+');
insert into course values(104,'python');
 select * from empp;
 select * from course;
 select * from empp
 inner	join course
 on empp.empp_id = course.empp_id;
  select * from empp
 left join course
 on empp.empp_id = course.empp_id;
SELECT empp.empp_id,
       empp.name,
       course.course
FROM empp
RIGHT JOIN course
ON empp.empp_id = course.emp_id;
ALTER TABLE empp
RENAME TO studentss;
SELECT
    studentss.name,
    course.course
FROM studentss
CROSS JOIN course;
DESCRIBE course;
SHOW COLUMNS FROM course;





